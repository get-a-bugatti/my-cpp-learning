#include <iostream>

int main()
{
    // Namespace - A namespace provides a solution for preventing name conflicts
    // especially in large projects.

    // Each entity in C++ requires a different name.
    // A namespace allows for identically named entities , as long as they have
    // a different NameSpace
}