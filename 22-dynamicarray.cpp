#include <iostream>
using namespace std;

int main() {
    // Pointers initialized dynamically are different in that std::size doesn't work on them.

    // Also , range loops don't work too . 
    // for (auto member : arr) {
    // }
    
    // ANd they lose properties of normal arrays.
}